import functions
import random

size = 9 # must be odd

map = functions.createEmpty(size)

generatorPosition = [2,2]
generatorDirection = random.randint(0,3) # from 0 to 3 clockwise, from the top

while True:
    pass

# print(functions.createMapPrint(map))